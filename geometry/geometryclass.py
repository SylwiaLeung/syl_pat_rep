class Geometry:

    # Determining the midpoint of a segment
    @staticmethod
    def middle_segment(point1: list, point2: list) -> tuple:
        A = (point1[0]+point2[0])/2
        B = (point1[1]+point2[1])/2
        return (A, B)

    # Checks for a collision between two rectangles

    def check_collision():
        pass